server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/YamilV18/INTIBYTES.git
          search-paths: config-data
          #username:
          #password:
          default-label: test
  security:
    user:
      name: root
      password: s3cr3t
